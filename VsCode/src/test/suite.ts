/*
 * Точка входа для `npm test`: импортирует все тестовые файлы.
 * Тесты регистрируются через node:test (describe/it) и выполняются
 * в текущем процессе — без спавна дочерних процессов раннера.
 */
import '../formats/latex/latexProvider.test.js';
